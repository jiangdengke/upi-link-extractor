# 必填：设置后才能启动 Compose。
UPI_ADMIN_PASSWORD=
UPI_SESSION_SECRET=
UPI_BIND_HOST=0.0.0.0
# 同时执行的账号提链任务数，服务端限制为 1-20。
UPI_MAX_CONCURRENCY=20
# 通过 HTTPS 访问时改为 1；直接使用 HTTP 时保持 0。
UPI_COOKIE_SECURE=0
# Docker 中由宿主机 Nginx 反代时填 *，确保绝对 URL 使用外部 HTTPS 地址。
UPI_FORWARDED_ALLOW_IPS=127.0.0.1
